language: node_js

node_js:
  - 6

install:
  - npm install -g hexo
  - npm install

script:
- hexo generate --deploy

deploy:
  provider: firebase
  token:
    secure: "YOUR ENCRYPTED token"
  project: "riversync-doc"